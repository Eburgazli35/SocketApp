/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  
    //output seçeneğini 'export' olarak ayarlamak, Next.js'in statik site olarak dışa aktarılmasını sağlar. 
    //Bu, Next.js uygulamanızı tamamen statik HTML, CSS ve JavaScript dosyalarına dönüştürür.
};

export default nextConfig;
